# Investor deck is hosted on Vercel Blob (DECK_URL) — never commit the PDF
assets/*.pdf

# Full-res headshot backups (originals kept locally, web versions are committed)
assets/*-orig.*

# Vercel / local
.vercel
.env
.env*.local
node_modules
.env*
